class Solution {
    public int minNumberOperations(int[] arr) {
        int total=0;
        for(int i=0;i<arr.length-1;i++){
            if(arr[i]>arr[i+1]){
                total+=arr[i]-arr[i+1];
            }
        }
        total+=arr[arr.length-1];
        return total;
    }
}
